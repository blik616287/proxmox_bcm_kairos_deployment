---
# Deploy Kairos raw image to BCM head node.
- name: Deploy Kairos via dd installer
  hosts: localhost
  gather_facts: true
  pre_tasks:
    - name: Load deployment facts
      ansible.builtin.include_tasks: load-deploy-facts.yml
  roles:
    - deploy_dd
