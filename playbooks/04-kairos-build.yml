---
# Build Kairos ISO via CanvOS + generate raw disk image.
- name: Build Kairos artifacts
  hosts: localhost
  gather_facts: true
  pre_tasks:
    - name: Load deployment facts
      ansible.builtin.include_tasks: load-deploy-facts.yml
  roles:
    - kairos_build
