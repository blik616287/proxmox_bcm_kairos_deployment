---
# Download BCM ISO, patch rootfs, remaster ISO for Proxmox auto-install.
- name: Prepare BCM auto-install ISO
  hosts: localhost
  gather_facts: true
  pre_tasks:
    - name: Load deployment facts
      ansible.builtin.include_tasks: load-deploy-facts.yml
  roles:
    - bcm_prepare
